# Dashboard reads .env from packages/contracts/.env (see packages/contracts/.env.example).
# This file is only a reference; put your real keys in packages/contracts/.env.

# WalletConnect / Reown (required for Connect button). Get free ID: https://cloud.walletconnect.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id

# Network the dashboard uses to read contracts (must match where your wallet is deployed)
NEXT_PUBLIC_CHAIN_ID=84532
NEXT_PUBLIC_RPC_URL=https://sepolia.base.org

# For local Anvil:
# NEXT_PUBLIC_CHAIN_ID=31337
# NEXT_PUBLIC_RPC_URL=http://127.0.0.1:8545

# AgentOS Agent Registry + Invoices (optional; required for /agents and /pay)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your_anon_key

# Agent API: optional server signer for POST /api/agent/transfers/propose (submit on behalf of agent)
# AGENTOS_SERVER_SIGNER_PRIVATE_KEY=0x... (64 hex chars; no server signer => returns prepared calldata)
